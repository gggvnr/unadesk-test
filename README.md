# Unadesk Test

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 21.2.2.

## Development server

To start a local development server, run:

```bash
ng serve
```

## Комментарии
Вынес отдельно репозитории для круда статей и аннотаций, но модели структурно остались в апи, надеюсь, не критично)

Тултип скопировал свой с минимальным клинапом, поэтому там много лишнего

Нейминг article/articles сильно путает, но переделывать уже не буду)

Колорпикер и инпуты по-хорошему тоже вынести, это осознанно не сделано от лени

По аннотациям:
 - сейчас попытка подогнать аннотации после редактирования текста ищет ближайшее совпадение, поэтому все сломается на одинаковых подстроках, но думаю, в тестовом не ожидается прям хорошая реализация)
 - можно еще смешивать цвета на пересекающихся сегментах или как-то еще их навешивать, этого тоже тут нет
 - выделение текста ожидаемо сбрасывается от чего угодно, как вариант - сразу в notes локально сохранять временную аннотацию без текста, чтоб визуально подсветить комментируемый участок
